micronaut:
    application:
        name: com-jagadish-server1
    server:
        port: 8095

eureka:
    client:
        registration:
            enabled: true
        defaultzone: "${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}"